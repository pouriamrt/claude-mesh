{
  "name": "@claude-mesh/peer-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "claude-mesh-peer-agent": "./dist/index.js", "mesh": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:ci": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('coverage',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@claude-mesh/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.1",
    "zod": "^3.23.8",
    "undici": "^6.20.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.2"
  }
}
